package com.Salesforce.qa.util;

import java.text.SimpleDateFormat;
import java.util.Date;

import com.aventstack.extentreports.ExtentReporter;
import com.aventstack.extentreports.ExtentReports;
import com.aventstack.extentreports.ExtentTest;
import com.aventstack.extentreports.reporter.ExtentHtmlReporter;



public class ReportUtil {

	/*static ExtentReports extent;
	 static ExtentTest logger ;
	 public static ReportUtil re=new ReportUtil();
		
	 //to create report: ExtentReports: It will create an html file
		void createReport(){
			String uniquedatetime=new SimpleDateFormat("'SampleDemo_'yyyyMMddHHmm''").format(new Date());
			String sPath=System.getProperty("user.dir")+"//ExtentReport//samplereport_"+uniquedatetime+".html";
			ExtentHtmlReporter reporter = new ExtentHtmlReporter(sPath);
			extent.attachReporter(reporter);
			extent = new ExtentReports();
			logger = extent.createTest("Test");
		}
		
		//start the testcase
		void createTestCase(String sTestCaseName) {
			logger=reports.startTest(sTestCaseName);
			
		}
		
		//
		void endTestCase() {
			reports.endTest(logger);
			
		}
		
		void endReport() {
			
			reports.flush();
			
		}
		
		
		
		*/
		

	}
